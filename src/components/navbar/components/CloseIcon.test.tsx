import React from 'react';
import { render } from '@testing-library/react';
import { CloseIcon } from './CloseIcon'; // Replace with the correct path

describe('CloseIcon', () => {
  it('renders without crashing', () => {
    render(<CloseIcon />);
  });
});
